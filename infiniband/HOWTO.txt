ONE TIME INTALLATION:
	python3 -m venv band_env;
	activate environment
	python3 -m pip install --upgrade conan






EXECUTION:

	execute 'memcached -vv -p 9999 on the memcached server node'
	execute 'export DORY_REGISTRY_IP={memcached server ip}:9999'
